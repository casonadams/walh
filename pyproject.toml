[project]
name = "walh"
version = "0.1.0"
description = "A vim colorscheme that consumes terminal ANSI colors"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
